{% extends 'base.html.twig' %}

{% block title %}Ajouter une série{% endblock %}

{% block body %}
    <h1>Ajouter une série</h1>

    {% if response != NULL %}
    {% else %}
    <span>Série non trouvée.<span>
    {% endif %}

    {{ include('series/_form.html.twig') }}

    <a href="{{ path('series_index') }}">Retour à la liste</a>
{% endblock %}
